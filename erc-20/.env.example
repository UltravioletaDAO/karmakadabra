# =============================================================================
# GLUE Token & Transaction Logger - Environment Configuration
# Karmacadabra - Ultravioleta DAO
# =============================================================================
#
# SECURITY WARNING: NEVER commit this file with real private keys!
# Use .env.example as template, copy to .env and fill in your values.
#
# =============================================================================

# -----------------------------------------------------------------------------
# Deployer Wallet - AWS SECRETS MANAGER
# -----------------------------------------------------------------------------

# Private keys are stored in AWS Secrets Manager (secret: 'karmacadabra')
#
# Setup (one-time):
#   1. Ensure ~/.aws credentials are configured
#   2. Run: python scripts/setup-secrets.py
#
# Local development override (optional):
#   - Leave PRIVATE_KEY empty to fetch from AWS Secrets Manager
#   - OR set PRIVATE_KEY=0x... to use local key (for testing/deployment)
#
# The deployment scripts will automatically:
#   - Use PRIVATE_KEY if filled in .env
#   - Otherwise, fetch from AWS Secrets Manager 'karmacadabra' secret
#
# Private key for deploying contracts (⚠️ USE TESTNET WALLET ONLY)
# This wallet deploys contracts and owns the tokens
PRIVATE_KEY=                                     # Leave empty to use AWS Secrets Manager

# -----------------------------------------------------------------------------
# Network Configuration
# -----------------------------------------------------------------------------

# Avalanche Fuji RPC endpoint
# Option 1: Use your custom/private RPC (recommended)
RPC_URL_AVALANCHE_FUJI=https://your-custom-rpc.xyz/avalanche-fuji

# Option 2: Use public RPC (fallback, may be rate-limited)
# RPC_URL_AVALANCHE_FUJI=https://avalanche-fuji-c-chain-rpc.publicnode.com

# Chain ID (Fuji testnet)
CHAIN_ID=43113

# -----------------------------------------------------------------------------
# Token Configuration (Hardcoded in Contract)
# -----------------------------------------------------------------------------
# These values are hardcoded in GLUE.sol contract:
#
# TOKEN_NAME="Gasless Lightweight Ultraviolet Economy"
# TOKEN_SYMBOL="GLUE"
# TOKEN_DECIMALS=6
# INITIAL_SUPPLY=24,157,817 GLUE
# OWNER_WALLET=0x52110a2Cc8B6bBf846101265edAAe34E753f3389
#
# Total supply with decimals: 24,157,817,000,000 (24.157817 million GLUE)

# -----------------------------------------------------------------------------
# Snowtrace Verification (Optional but Recommended)
# -----------------------------------------------------------------------------

# Snowtrace API key for contract verification
# Get your API key at: https://snowtrace.io/myapikey
SNOWTRACE_API_KEY=YOUR_SNOWTRACE_API_KEY_HERE

# -----------------------------------------------------------------------------
# Deployment Checklist:
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Edit .env and set:
#    - PRIVATE_KEY (testnet wallet with ~0.5 AVAX for deployment gas)
#    - RPC_URL_AVALANCHE_FUJI (custom RPC recommended)
#    - SNOWTRACE_API_KEY (optional, for verification)
#
# 3. Get testnet AVAX:
#    https://faucet.avax.network/
#
# 4. Install dependencies:
#    forge install
#
# 5. Build:
#    forge build
#
# 6. Deploy:
#    ./deploy-fuji.sh
#
# 7. Save deployment address from deployment.json to:
#    ../x402-rs/.env (UVD_TOKEN_ADDRESS)
#
# =============================================================================
